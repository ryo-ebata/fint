use crate::Fint;

impl From<i32> for Fint {
    fn from(item: i32) -> Self {
        Fint::new(item)
    }
}

impl From<i8> for Fint {
    fn from(item: i8) -> Self {
        Fint::new(item)
    }
}

impl From<i16> for Fint {
    fn from(item: i16) -> Self {
        Fint::new(item)
    }
}

impl From<i64> for Fint {
    fn from(item: i64) -> Self {
        Fint::new(item)
    }
}

impl From<i128> for Fint {
    fn from(item: i128) -> Self {
        Fint::new(item)
    }
}

impl From<u8> for Fint {
    fn from(item: u8) -> Self {
        Fint::new(item)
    }
}

impl From<u16> for Fint {
    fn from(item: u16) -> Self {
        Fint::new(item)
    }
}

impl From<u32> for Fint {
    fn from(item: u32) -> Self {
        Fint::new(item)
    }
}

impl From<u64> for Fint {
    fn from(item: u64) -> Self {
        Fint::new(item)
    }
}

impl From<u128> for Fint {
    fn from(item: u128) -> Self {
        Fint::new(item)
    }
}
